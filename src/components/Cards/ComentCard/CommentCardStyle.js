import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container:{
    margin:10,
    padding:10,
    borderBottomWidth:1
  },
  text:{
    fontSize:15,
    fontWeight:"800",
    paddingBottom:5
  }
})

export default styles