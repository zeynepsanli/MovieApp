import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        alignItems: "center"
    },
    text: {
        fontSize: 15,
        color: "silver"
    }
})
export default styles